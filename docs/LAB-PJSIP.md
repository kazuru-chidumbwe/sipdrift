# PJSIP lab driver

See [`LAB-DRIVERS.md`](LAB-DRIVERS.md) for the current `pjsip-lab` / `sofia-lab` setup.

Historical note: early drafts assumed a `pjsua` subprocess. The shipped helper uses `pjsip_parse_msg` via `tools/pjsip_observe`.
